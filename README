This is Perl module Color::Convert::ANSIColor.

INSTALLATION

Color::Convert::ANSIColor installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Color::Convert::ANSIColor

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Color::Convert::ANSIColor documentation is available as in POD. So you can do:

    % perldoc Color::Convert::ANSIColor

to read the documentation online with your favorite pager.

dameninngenn
